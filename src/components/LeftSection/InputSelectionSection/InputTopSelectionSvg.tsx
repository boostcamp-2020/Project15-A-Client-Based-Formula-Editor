import React from 'react';
import * as StyledComponent from './style';

interface SvgProps {
  Svg: string;
}
const InputTopSelectionSection = ({ Svg }: SvgProps) => {
  return <Svg />;
};

export default InputTopSelectionSection;
